<% # copied from http://shiningthrough.co.uk/Dynamic-multiple-image-uploads-with-Ruby-on-Rails -%>
<fieldset>
  <% if !f.object.new_record? %>
    <%= image_tag f.object.image.url %>
  <% end %>

  <%= f.file_field :image %>
  <%= delete_photo(f) %>
</fieldset>
